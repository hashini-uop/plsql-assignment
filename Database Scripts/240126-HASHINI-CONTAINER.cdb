CREATE TABLE HASHINI_CONTAINER_TAB (
    container_id NUMBER,
    current_weight NUMBER,
    max_volume NUMBER,
    max_weight NUMBER,
    current_volume NUMBER,
    
    branch_id NUMBER,
    
    PRIMARY KEY(branch_id, container_id )

);
