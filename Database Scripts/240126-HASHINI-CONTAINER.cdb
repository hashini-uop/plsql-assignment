CREATE TABLE HASHINI_CONTAINER_TAB (
    container_id NUMBER,
    current_weight NUMBER,
    max_volume NUMBER,
    max_weight NUMBER,
    current_volume NUMBER,
    
    branch_id NUMBER,
    company_reg_no NUMBER,
    
    PRIMARY KEY(branch_id, container_id ,company_reg_no)

);

CREATE SEQUENCE   HASHINI_CONATAINER_SEQ
  START WITH 1
  INCREMENT BY 1
  NOCACHE
  NOCYCLE;